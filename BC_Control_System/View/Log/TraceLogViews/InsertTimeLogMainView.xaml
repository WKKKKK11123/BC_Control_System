<UserControl
    x:Class="BC_Control_System.view.Log.TraceLogViews.InsertTimeLogMainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:BC_Control_System.View.Log"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="900"
    d:DesignWidth="1348"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="176" />
        </Grid.ColumnDefinitions>
        <Border
            Margin="6"
            BorderBrush="Gray"
            BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="30*" />
                </Grid.RowDefinitions>
                <TextBlock Margin="10,10,1,1" Text="{Binding ModuleName,Mode=TwoWay}" />
                <Border Grid.Row="1" Margin="4,10,4,1">
                    <ContentControl
                        Name="TraceLogContent"
                        Grid.Row="1"
                        prism:RegionManager.RegionName="TraceLogContentRegion" />
                </Border>
            </Grid>
        </Border>
        <Border
            Grid.Column="2"
            Margin="6"
            BorderBrush="Gray"
            BorderThickness="1">
            <StackPanel Grid.Column="1">
                <Button
                    Width="140"
                    Margin="5,5"
                    Command="{Binding SelectTimeCommand}"
                    Content="Search" />
                <Button
                    Width="140"
                    Margin="5,5"
                    Content="Save" />
                <Button
                    Width="140"
                    Margin="5,5"
                    Command="{Binding OpenChartViewCommand}"
                    Content="Graph" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
