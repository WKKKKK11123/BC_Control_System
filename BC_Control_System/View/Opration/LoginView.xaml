<UserControl
    x:Class="BC_Control_System.View.Opration.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:BC_Control_System.View.Opration"
    xmlns:localCommand="clr-namespace:BC_Control_System.Command"
    xmlns:localConverters="clr-namespace:BC_Control_System.Converters"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:theme="clr-namespace:BC_Control_System.Theme"
    d:DesignHeight="340"
    d:DesignWidth="600"
    theme:ColorThemeProps.Background="{DynamicResource BackgroundBrush}"
    Background="White"
    mc:Ignorable="d">
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <!--  窗体的宽和高根据内容的大小设置  -->
            <Setter Property="SizeToContent" Value="WidthAndHeight" />
            <!--  不允许调整窗体尺寸  -->
            <Setter Property="ResizeMode" Value="NoResize" />
            <!--  设置窗体的启动位置在屏幕中间  -->
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterScreen" />
        </Style>
    </prism:Dialog.WindowStyle>
    <Border
        Height="180"
        Padding="16"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        BorderBrush="Gray"
        BorderThickness="0"
        CornerRadius="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="200" />
            </Grid.ColumnDefinitions>
            <TextBlock
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="16"
                Text="Username" />
            <TextBox Grid.Column="1" Text="{Binding UsernameValue}" />
            <TextBlock
                Grid.Row="2"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="16"
                Text="Password" />
            <PasswordBox
                Grid.Row="2"
                Grid.Column="1"
                localCommand:PasswordBoxHelper.Attach="True"
                localCommand:PasswordBoxHelper.Password="{Binding PasswordValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <StackPanel
                Grid.Row="5"
                Grid.ColumnSpan="2"
                Orientation="Horizontal">
                <Button
                    Width="100"
                    Margin="20,0,40,0"
                    Command="{Binding ConfirmCommand}"
                    Content="Login" />
                <Button
                    Width="100"
                    Command="{Binding CancelCommand}"
                    Content="Exit" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
