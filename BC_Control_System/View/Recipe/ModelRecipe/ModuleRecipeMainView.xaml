<UserControl
    x:Class="BC_Control_System.View.Recipe.ModelRecipe.ModuleRecipeMainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behavior="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:conv="clr-namespace:ValueConverters;assembly=ValueConverters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:BC_Control_System.View.Recipe.ModelRecipe"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:rule="clr-namespace:BC_Control_System.Rule"
    d:DesignHeight="930"
    d:DesignWidth="1900"
    mc:Ignorable="d">
    <UserControl.Resources>
        <conv:ValueConverterGroup x:Key="BoolToVisible">
            <conv:BoolInverter FalseValue="True" TrueValue="False" />
            <conv:BoolToVisibilityConverter />
        </conv:ValueConverterGroup>
        <Style TargetType="TextBox">
            <!--  默认状态：透明背景  -->
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Gray" />
            <Setter Property="BorderThickness" Value="1" />

            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="Background" Value="White" />
                </Trigger>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip" Value="{Binding RelativeSource={x:Static RelativeSource.Self}, Path=(Validation.Errors)[0].ErrorContent}" />
                </Trigger>
            </Style.Triggers>
        </Style>


    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20*" />
            <ColumnDefinition Width="176" />
        </Grid.ColumnDefinitions>
        <Border Grid.Row="1" Margin="4,10,4,1">
            <ContentControl
                Name="ModuleRecipeContent"
                Grid.Column="1"
                prism:RegionManager.RegionName="ModuleRecipeContentRegion" />
        </Border>
        <Border
            Grid.Column="2"
            Margin="6"
            BorderBrush="Gray"
            BorderThickness="1">
            <StackPanel VerticalAlignment="Top" Orientation="Vertical">
                <Button
                    Width="140"
                    Margin="5,5"
                    VerticalAlignment="Center"
                    Command="{Binding NewRecipeCommand}"
                    Content="New" />
                <Button
                    Width="140"
                    Margin="5,5"
                    VerticalAlignment="Center"
                    Command="{Binding OpenFileCommand}"
                    Content="Open" />
                <Button
                    Width="140"
                    Margin="7,5"
                    VerticalAlignment="Center"
                    Command="{Binding SaveFileCommand}"
                    Content="Save" />
                <Button
                    Width="140"
                    Margin="5,5"
                    VerticalAlignment="Center"
                    Command="{Binding DeleteFileCommand}"
                    Content="Delete" />
                <Button
                    Width="140"
                    Margin="5,5"
                    VerticalAlignment="Center"
                    Command="{Binding AddPreStepCommand}"
                    Content="Add Pre Step" />
                <Button
                    Width="140"
                    Margin="5,5"
                    VerticalAlignment="Center"
                    Command="{Binding AddStepCommand}"
                    Content="Add Step" />
                <Button
                    Width="140"
                    Margin="5,5"
                    VerticalAlignment="Center"
                    Command="{Binding AddPostStepCommand}"
                    Content="Add Post Step" />
                <Button
                    Width="140"
                    Margin="5,5"
                    VerticalAlignment="Center"
                    Command="{Binding InsertStepCommand}"                  
                    Content="Ins Step" />
                <Button
                    Width="140"
                    Margin="5,5"
                    VerticalAlignment="Center"
                    Command="{Binding DeleteStepCommand}"
                    Content="Del Step" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
